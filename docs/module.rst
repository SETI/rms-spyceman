``spyceman`` Module
===================

.. The API reference is empty for now. ``automodule`` has to import the package, and
   ``import spyceman`` currently raises SyntaxError from
   ``src/spyceman/_downloads.py``. Uncomment the block below once that is repaired, and
   add the host and solarsystem modules alongside it.

.. Once the package imports:

    .. automodule:: spyceman
        :member-order: bysource
        :members:
        :undoc-members:
        :special-members:
        :exclude-members: __dict__, __hash__, __module__, __weakref__, __annotations__, __abstractmethods__

    ``spyceman.solarsystem.Saturn`` Module
    ======================================

    .. automodule:: spyceman.solarsystem.Saturn
        :members:

The package does not yet import, so there is no generated API documentation on this
page. See the project README for the intended interface.
